{
  "name": "telegram-tts-bot-deployment",
  "version": "1.0.0",
  "description": "Deployment configuration for Telegram TTS Bot",
  "main": "project/bot.js",
  "private": true,
  "scripts": {
    "start": "node project/bot.js",
    "dev": "node project/bot.js",
    "build": "cd project && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {}
}